---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/feed.xml' | absolute_url }}</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% assign blog_pages = site.pages | where: "layout", "post" | sort: "date" | reverse %}
  {% for post in blog_pages %}
    {% assign publish_date = post.date | default: post.Date | default: site.time %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
    <published>{{ publish_date | date_to_xmlschema }}</published>
    <updated>{{ publish_date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url }}</id>
    {% if post.description %}
    <summary type="html">{{ post.description | xml_escape }}</summary>
    {% endif %}
    <content type="html">{{ post.content | markdownify | strip_html | normalize_whitespace | xml_escape }}</content>
  </entry>
  {% endfor %}
</feed>
